<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <%= link_to 'Hospital Management System', root_path, class: 'navbar-brand' %>
        <% if current_user %> 
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                  <%# <li class="nav-item <%= 'active' if request.path == departments_path %>
                      <%# <%= link_to 'Department', departments_path, class: 'nav-link' %>
                  <%# </li> %> 
                  <li class="nav-item <%= 'active' if request.path == appointments_path %>">
                      <%= link_to 'Appointment', appointments_path, class: 'nav-link' %>
                  </li>
                  <li class="nav-item <%= 'active' if request.path == prescriptions_path %>">
                      <%= link_to 'Prescription', prescriptions_path, class: 'nav-link' %>
                  </li>
                  
                </ul>
                
                <ul class='navbar-nav ml-auto'>
                <%# <%- if logged_in? %> 
                  <li class="nav-item">
                    <a class="nav-link" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">

                    
                    <%# <%= current_user.user_name %>
                    <%# <% end %>
                    </a>
                
                    <li class='nav-item'>
                        <%= link_to("Logout", destroy_user_session_path,
                            class: "btn btn-danger logout-button", method: :delete)%>
                    </li>
                </ul>
                 
            </div>
        <% end %>
    </div>
</nav>